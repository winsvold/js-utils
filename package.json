{
  "name": "js-utils",
  "version": "0.0.1",
  "description": "A small utility-library for reusable code in Biblioteksentralens js-apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist && tsc",
    "prepublish": "tsc"
  },
  "author": "Biblioteksentralen",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "husky": "^7.0.2",
    "jest": "^27.2.0",
    "prettier": "^2.4.0",
    "typescript": "^4.4.3"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  },
  "prettier": {
    "printWidth": 120
  },
  "lint-staged": {
    "*.{js,jsx,json,md,ts,tsx}": "prettier --write"
  },
  "dependencies": {
    "date-fns": "^2.23.0",
    "ts-jest": "^27.0.5"
  }
}
